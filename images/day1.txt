



set serveroutput on;
DECLARE
A NUMBER(10) := 20;
B NUMBER(10) := 30;
c number(10);




BEGIN
c:=a+b;
DBMS_OUTPUT.PUT_LINE('The result after addition is :||'c);

END;



create table employee(ename varchar2(10),empid number(10),salary number(10),location varchar2(10));

insert into employee values('ram',23,23000,'ldh');
insert into employee values('sham',18,25000,'delhi');
insert into employee values('sita',17,20000,'pnb');


DECLARE 
v_empid employee.empid%type := &empid;
v_ename employee.ename%type ;
v_salary employee.salary%type;
v_location employee.location%type;



BEGIN
select ename,salary,location into v_ename,v_salary,v_location from employee where empid=v_empid;
dbms_output.put_line('The employee ' || v_ename || 'has employee id as ' || v_empid);
end;


--%ROWTYPE

DECLARE

V_EMPID  EMPLOYEE.EMPID%TYPE := &EMPID;
EMP_REC  EMPLOYEE%ROWTYPE;

BEGIN 
SELECT ENAME,EMPID,SALARY,LOCATION INTO EMP_REC FROM EMPLOYEE
WHERE EMPID=V_EMPID;


DBMS_OUTPUT.PUT_LINE(EMP_REC.ENAME);
DBMS_OUTPUT.PUT_LINE(EMP_REC.EMPID);
DBMS_OUTPUT.PUT_LINE(EMP_REC.SALARY);
DBMS_OUTPUT.PUT_LINE(EMP_REC.LOCATION);
END;

--BIND VARIABLE

VARIABLE RESULT NUMBER;


BEGIN
SELECT SALARY*12 INTO :RESULT FROM EMPLOYEE WHERE EMPID=18;
DBMS_OUTPUT.PUT_LINE('CHANGED SALARY IS' || :RESULT);
END;


PRINT RESULT;


--RECORD TYPE VARIABLE

DECLARE
TYPE MY_REC IS RECORD
(ENO EMPLOYEE.EMPID%TYPE,
ENAME EMPLOYEE.ENAME%TYPE,
SAL EMPLOYEE.SALARY%TYPE);
EMP_REC MY_REC;

BEGIN
SELECT EMPID,ENAME,SALARY INTO 
EMP_REC.ENO,EMP_REC.ENAME,EMP_REC.SAL FROM EMPLOYEE WHERE EMPID=18;
DBMS_OUTPUT.PUT_LINE('EMPLOYEE NUMBER IS' || EMP_REC.ENO);
DBMS_OUTPUT.PUT_LINE('EMPLOYEE NAME IS' || EMP_REC.NAME);
DBMS_OUTPUT.PUT_LINE('SALRY IS '||EMP_REC.SAL);
END;


--insert statement

DECLARE
V_EMPNO EMPLOYEE.EMPID%TYPE:=&EMPNO;
V_ENAME EMPLOYEE.ENAME%TYPE:='&ENAME';
V_SALARY EMPLOYEE.SALARY%TYPE:=&SALARY;
V_LOCATION EMPLOYEE.LOCATION%TYPE:='&LOCATION';

BEGIN


INSERT INTO EMPLOYEE VALUES(V_ENAME,V_EMPNO,V_SALARY,V_LOCATION);
COMMIT;
END;


--UPDATE STATEMENT

DECLARE
V_EMPNO EMPLOYEE.EMPNO%TYPE;
V_SALARY EMPLOYEE.SALARY%TYPE:=&SAL;

BEGIN

UPDATE EMPLOYEE SET SALARY=V_SALARY WHERE EMPID=13;
COMMIT;
END;


--DELETE

DECLARE
V_EMPNO EMPLOYEE.EMPID%TYPE:=&EMPNO;

BEGIN
DELETE EMPLOYEE WHERE EMPID=V_EMPNO;
COMMIT;
END;

--SEQUENCE

CREATE SEQUENCE SEQ_EMP;

DECLARE
V_EMPNO EMPLOYEE.EMPID%TYPE;
BEGIN
INSERT INTO EMPLOYEE(EMPID,ENAME) VALUES(SEQ_EMP.NEXTVAL,'AJAY')
RETURNING EMPID INTO V_EMPNO;
DBMS_OUTPUT.PUT_LINE(V_EMPNO);
END;



--if then endif

DECLARE 
 A NUMBER(10):=20;
B NUMBER(10):=30;

BEGIN

IF A > B
THEN DBMS_OUTPUT.PUT_LINE('NUMBER A GREATER THEN B');

ELSE
DBMS_OUTPUT.PUT_LINE('B GREATER');
END IF;

END;



DECLARE
V_ENAME EMPLOYEE.ENAME%TYPE;
V_EMPID EMPLOYEE.EMPID%TYPE:=&EMPID;
V_SAL EMPLOYEE.SALARY%TYPE;
BEGIN
SELECT ENAME,SALARY INTO V_ENAME,V_SAL FROM EMPLOYEE
WHERE EMPID=V_EMPID;
IF V_SAL > 2500
THEN
DBMS_OUTPUT.PUT_LINE(V_ENAME || 'EARNS SALARY GREATER THEN 2500);
ELSE
DBMS_OUTPUT.PUT_LINE(V_ENAME|| 'EARNS SALARY LESS THEN 2500');
END IF;
END;









